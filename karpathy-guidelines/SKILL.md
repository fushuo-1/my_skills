---
name: karpathy-guidelines
description: AI编码行为指南，减少LLM编码的常见错误。在编写、编辑、审查或重构任何代码时使用此技能。当用户请求代码修改、功能实现、bug修复、代码审查、重构或任何涉及代码变更的任务时，都应该应用这些原则。即使用户没有明确提及，也应在每次编码任务中自动遵循。
---

# Karpathy AI 编码指南

源自 Andrej Karpathy 对 LLM 编码陷阱的观察，四条核心原则用于减少 AI 编码中的常见错误。

**权衡：** 倾向于谨慎而非速度。琐碎任务（拼写修复、显而易见的一行改动）可自行判断，不必走完整流程。

---

## 1. 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

实现前，先想清楚再动手：

- **明确说明假设** — 如果不确定，询问而非猜测。不要默默选一种解释然后执行。
- **呈现多种解释** — 当存在歧义时，把选项摆出来让用户选择。
- **适时提出异议** — 如果存在更简单的方案，说出来。用户提的方案不一定是最好的。
- **困惑时停下来** — 指出不清楚的地方并请求澄清。带着困惑硬写比停下来问更浪费时间。

为什么要这样做：LLM 的一个核心问题是会默默做错误假设然后不假思索地执行。这条原则强制在动手前暴露认知盲区。

---

## 2. 简洁优先

**用最少的代码解决问题。不做推测性实现。**

- 不添加需求之外的功能（YAGNI）
- 不为一次性代码创建抽象层
- 不添加未要求的"灵活性"或"可配置性"
- 不为不可能发生的场景做错误处理
- 如果写了 200 行发现 50 行就能搞定，重写它

**自检标准：** 资深工程师会觉得这过于复杂吗？如果是，简化。

为什么要这样做：LLM 天生倾向过度工程化，明明简单的需求非要搞成臃肿架构。100 行能解决的事情不要写成 1000 行。

---

## 3. 精准修改

**只碰必须碰的。只清理自己造成的混乱。**

编辑现有代码时：

- 不要"顺便改进"相邻的代码、注释或格式
- 不要重构没坏的东西
- 匹配现有代码风格，即使你偏好不同的写法
- 如果注意到无关的死代码，提一下——但不要删除它

当改动产生孤儿代码时：

- 删除因你的改动而变得无用的导入/变量/函数（你制造的混乱你清理）
- 不要删除预先存在的死代码，除非被要求

**检验标准：** 每一行修改都应该能直接追溯到用户的请求。如果用户问"改个按钮颜色"，diff 里不应该出现重构和格式化。

为什么要这样做：LLM 经常改动或删除自己理解不足的代码和注释，即使这些内容与任务无关。精准修改把爆炸半径降到最小。

---

## 4. 目标驱动执行

**定义成功标准。循环验证直到达成。**

将模糊的指令转化为可验证的目标：

| 模糊指令 | 转化为 |
|---------|--------|
| "添加验证" | "为无效输入编写测试，然后让它们通过" |
| "修复 bug" | "编写能重现 bug 的测试，然后让它通过" |
| "重构 X" | "确保重构前后测试都能通过" |

多步骤任务，列出简短计划：

```
1. [步骤] → 验证: [检查什么]
2. [步骤] → 验证: [检查什么]
3. [步骤] → 验证: [检查什么]
```

**强有力的成功标准**让 AI 能独立循环执行直到达成。**弱标准**（"让它工作"）则需要不断来回澄清。

为什么要这样做：Karpathy 指出"LLM 非常擅长循环执行直到达成特定目标"。给目标而不是给指令，效果更好。

---

## 生效标志

这些指南正在发挥作用时，你会看到：

- diff 中不必要的改动更少 — 只有请求的改动出现
- 因过度复杂而导致的重写更少 — 代码第一次就写得简洁
- 澄清问题在实现之前提出 — 而不是在犯错之后
- 干净、精简的变更 — 没有顺带的重构或"改进"
